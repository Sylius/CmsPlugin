{% set collection = hookable_metadata.context.collection %}

<a href="{{ path('sylius_cms_shop_collections_page_index', {'code' : collection.code}) }}"  {{ sylius_test_html_attribute('cms-page-collection-link') }}>{{ collection.name }}</a>

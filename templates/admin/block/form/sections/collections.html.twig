{% set form = hookable_metadata.context.form %}

<div class="card mb-3" {{ sylius_test_html_attribute('collections') }}>
    <div class="card-body">
        {{ form_row(form.collections, sylius_test_form_attribute('collection-autocomplete')) }}
    </div>
</div>
